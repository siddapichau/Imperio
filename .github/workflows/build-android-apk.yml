ps://github.com/siddapichau/Imperio/pull/7

Importante: o PR subiu com toda a preparação do APK e logo roxa vinho, mas não consegui incluir o workflow do GitHub Actions no PR porque o GitHub bloqueou a permissão workflows.

Eu já deixei aberto para você baixar o arquivo:

.github/workflows/build-android-apk.yml

Para colocar manualmente no GitHub:

Abra o repositório no GitHub.
Clique em Add file → Create new file.
No nome do arquivo coloque exatamente:
txt

.github/workflows/build-android-apk.yml
Cole o conteúdo do arquivo que eu abri para você baixar.
Clique em Commit changes direto na main.
Depois disso, quando você fizer merge do PR, o GitHub Actions vai gerar o APK e ele aparecerá em Actions → Build Android APK → Artifacts com o nome:

txt

Igreja-Imperial-Batista-APK-roxo-vinho
Se preferir liberar a permissão para eu subir o workflow direto no PR, reconecte o GitHub no Arena com permissão de Workflows/Actions, me avise, e eu adiciono esse arquivo no PR automaticamente.


Esta tarefa foi bem-sucedida?


Sim

Não

Continuar trabalhando
Arena | Benchmark & Compare the Best AI Models
Workspace

Diff

Branch
unified
split


.github/workflows/build-android-apk.yml
+64
name: Build Android APK

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
      - arena/019f9c23-imperio
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-apk:
    name: Gerar APK Android
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - name: Baixar código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Configurar Android SDK
        uses: android-actions/setup-android@v3

      - name: Instalar SDK Android 36
        run: |
          yes | sdkmanager --licenses >/dev/null
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Instalar dependências
        run: npm ci

      - name: Gerar APK com logo roxa vinho
        run: npm run android:build:debug

      - name: Separar APK para download
        run: |
          mkdir -p artifacts
          cp android/app/build/outputs/apk/debug/app-debug.apk "artifacts/Igreja-Imperial-Batista-roxo-vinho-debug.apk"
          ls -lh artifacts

      - name: Publicar APK nos artifacts do GitHub Actions
        uses: actions/upload-artifact@v4
        with:
          name: Igreja-Imperial-Batista-APK-roxo-vinho
          path: artifacts/*.apk
          if-no-files-found: error
          retention-days: 30
